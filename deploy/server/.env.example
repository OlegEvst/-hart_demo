# Конфигурация API сервера

# Порт сервера
PORT=3001

# Режим работы
NODE_ENV=production

# Корневая директория проекта (где находятся src/data/ и src/components/)
# ВАЖНО: Укажите абсолютный путь к директории charts-demo на сервере
PROJECT_ROOT=/var/www/charts-demo

# Альтернативно, можно указать отдельные пути:
# DATA_DIR=/var/www/charts-demo/src/data
# COMPONENTS_DIR=/var/www/charts-demo/src/components

# Директория для статических файлов (опционально)
# STATIC_DIR=/var/www/charts/static
