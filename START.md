# 🚀 Инструкция по запуску проекта

## Быстрый старт

### Вариант 1: Использовать скрипт (рекомендуется)

```bash
cd charts-demo
bash start-servers.sh
```

### Вариант 2: Запустить вручную

#### 1. Запуск Backend сервера (порт 3001)

Откройте первый терминал:

```bash
cd charts-demo/server
npm start
```

Backend будет доступен на:
- API: http://localhost:3001/api
- Админка: http://localhost:3001/admin/graph_builder

#### 2. Запуск Frontend сервера (порт 5173)

Откройте второй терминал:

```bash
cd charts-demo
npm run dev
```

Frontend будет доступен на:
- http://localhost:5173

## 📋 Проверка работы

1. **Frontend**: Откройте http://localhost:5173 в браузере
2. **Backend API**: Проверьте http://localhost:3001/api/charts/list
3. **Админка**: Откройте http://localhost:3001/admin/graph_builder

## 🛑 Остановка серверов

Нажмите `Ctrl+C` в терминалах, где запущены серверы.

Или используйте:
```bash
# Найти процессы
lsof -ti:3001,5173

# Остановить процессы
kill $(lsof -ti:3001,5173)
```

## 📝 Примечания

- Backend сервер должен быть запущен первым
- Frontend автоматически подключается к backend на порту 3001
- Все данные уже загружены в проект (518 графиков)
- Новые графики добавлены и готовы к использованию
